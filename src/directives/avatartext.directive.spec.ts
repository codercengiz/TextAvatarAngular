import { AvatartextDirective } from './avatartext.directive';

describe('AvatartextDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatartextDirective();
    expect(directive).toBeTruthy();
  });
});
